#
#  project.yml — XcodeGen spec for Calorisor
#
#  Regenerate the .xcodeproj after changing this file (or adding/removing sources):
#      xcodegen generate
#
#  Both project.yml and the generated Calorisor.xcodeproj are committed, so the app can
#  be opened directly without XcodeGen installed.
#

name: Calorisor

options:
  bundleIdPrefix: com.fatih
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
  developmentLanguage: tr
  groupSortPosition: top

# Swift Package Manager dependencies.
# - Pow: SwiftUI micro-effect + transition library (EmergeTools). Used in LogButton
#   for haptic feedback + checkmark particle burst on successful log. iOS 15+.
# - Lottie: vector animation renderer. Used in paywall/empty-state hero. iOS 13+.
#   (Phase 2 — only declared here, no consumer yet, comment kept to track the plan.)
#
# Lottie SPM pin: use the precompiled `lottie-spm` mirror (8MB), NOT the main
# `airbnb/lottie-ios` repo (300+MB) — SPM would otherwise download the full
# git history every resolve.
packages:
  Pow:
    url: https://github.com/EmergeTools/Pow
    from: 1.0.5
  Lottie:
    url: https://github.com/airbnb/lottie-spm.git
    branch: main

settings:
  base:
    MARKETING_VERSION: "1.0.0"
    CURRENT_PROJECT_VERSION: "1"
    SWIFT_VERSION: "5.0"
    DEVELOPMENT_TEAM: "8XPP7Z37GF"
    SWIFT_EMIT_LOC_STRINGS: "YES"
    ENABLE_USER_SCRIPT_SANDBOXING: "YES"

targets:
  Calorisor:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: Calorisor
        excludes:
          - "**/*.md"
          - "Info.plist"
          - "Secrets.plist"
          - "*.entitlements"
    settings:
      base:
        PRODUCT_NAME: Calorisor
        PRODUCT_BUNDLE_IDENTIFIER: com.fatih.calorisor
        INFOPLIST_FILE: Calorisor/Info.plist
        CODE_SIGN_ENTITLEMENTS: Calorisor/Calorisor.entitlements
        GENERATE_INFOPLIST_FILE: "NO"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        ENABLE_PREVIEWS: "YES"
        TARGETED_DEVICE_FAMILY: "1"
        CODE_SIGN_STYLE: Automatic
        INFOPLIST_KEY_NSHealthShareUsageDescription: "Calorisor reads weight, height, active energy, and steps to personalize your nutrition view."
        INFOPLIST_KEY_NSHealthUpdateUsageDescription: "Calorisor writes meal calories and macros to Health."
    dependencies:
      - target: CalorisorWidgetExtension
      - package: Pow
        product: Pow
      - package: Lottie
        product: Lottie
    scheme:
      testTargets:
        - CalorisorTests
      gatherCoverageData: false
      storeKitConfiguration: Calorisor/StoreKit/Products.storekit

  CalorisorTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: CalorisorTests
    dependencies:
      - target: Calorisor
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.fatih.calorisor.tests
        GENERATE_INFOPLIST_FILE: "YES"
        TARGETED_DEVICE_FAMILY: "1"

  CalorisorWidgetExtension:
    type: app-extension
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: CalorisorWidget
      - path: Calorisor/Models/WidgetDailySummary.swift
      - path: Calorisor/Models/WidgetDataStore.swift
      - path: Calorisor/Assets.xcassets
    settings:
      base:
        PRODUCT_NAME: "Calorisor Widget"
        PRODUCT_BUNDLE_IDENTIFIER: com.fatih.calorisor.widget
        INFOPLIST_FILE: CalorisorWidget/Info.plist
        CODE_SIGN_ENTITLEMENTS: CalorisorWidget/CalorisorWidget.entitlements
        GENERATE_INFOPLIST_FILE: "NO"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        TARGETED_DEVICE_FAMILY: "1"
        CODE_SIGN_STYLE: Automatic
